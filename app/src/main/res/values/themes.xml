<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Только фон окна до первой отрисовки: всё остальное рисует Compose. -->
    <style name="Theme.TvRemote" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
